package com.beautystore.dto.request.filter.criteria;

public enum CommoditySearchCriteria {
    BY_BRAND,
    BY_SUBCATEGORY,
    BY_NAME,
    BY_PRICE_BETWEEN,
    BY_PRICE_LESS_THEN,
    BY_PRICE_MORE_THEN
}
